// ---------------------------------------------------------------------------
// ProfilePage — re-export wrapper for /profile/:id route
// ---------------------------------------------------------------------------

import ProfilePage from "../../components/pages/profile/UserProfile";

export default ProfilePage;
